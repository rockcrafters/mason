Use the chisel-releases skill to author chisel slice definition files (SDFs) for these packages on the `ubuntu-26.04` branch: freeipmi-common, libfreeipmi17, ipmitool.

You are in a checkout of canonical/chisel-releases (your current directory). These slices do not exist yet -- create `slices/<package>.yaml` for each of the three packages. Follow the skill's write-slice workflow and the conventions in the skill's shared reference, including any inter-package dependencies between them.

This is an automated eval: work autonomously, do not ask questions, and do not open a PR. Produce the slice files and stop.
